If both the test pak AND the upgrade fail at APPLY_ADAPTER, the issue isn't the content/kind mismatch — it's something in the pak itself. I need to see the actual error.

Grab the apply_adapter log from the Aria Ops node:


sudo tail -100 /storage/log/vcops/log/pakManager/vcopsPakManager.root.apply_adapter.log
Paste what comes back — that log has the specific reason APPLY_ADAPTER rejected the pak.

Also worth checking:


# Show the tail of the ADAPTER app log if it made it that far
sudo tail -50 /storage/log/vcops/log/adapter3/adapter3.log 2>/dev/null | grep -iE "azure|microsoft|error" | tail -20

# And the describe.xml schema validation log if any
sudo grep -iE "describe|schema|azure" /storage/log/vcops/log/pakManager/vcopsPakManager.root.apply_adapter*.log | tail -20
While you pull those, a couple of hypotheses I want to test:

1. describe.xml might have invalid schema content. Our patch-describe-xml.py adds attributes (type, subType, worldObjectName, showTag, PowerState) after mp-build generates the XML. If those attributes aren't allowed by the schema at the positions we insert them, Aria Ops rejects the whole pak. The test pak hit the same step, so this is my top suspect.

2. Something in adapter.py's get_adapter_definition() produces invalid output. The DuplicateKeyException we fixed earlier was one such issue — there might be more that only surface during Aria Ops' strict validation (vs. mp-test's looser check).

Quick verification step — extract the pak we just built and sanity-check the describe.xml locally:


cd /tmp && rm -rf pak-check
mkdir pak-check && cd pak-check
unzip -q /opt/aria/Aria-MP-Builder/Azure/build/MicrosoftAzureAdapter_8.19.0.pak
unzip -q adapter.zip
ls MicrosoftAzureAdapter/conf/
xmllint --noout --schema MicrosoftAzureAdapter/conf/describeSchema.xsd MicrosoftAzureAdapter/conf/describe.xml 2>&1 | head -30
xmllint will tell us if describe.xml is schema-valid. If it reports errors, that's your answer.

Paste the log output and the xmllint output — one of them will pinpoint it.