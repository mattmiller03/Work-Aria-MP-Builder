cd /tmp/adapter-check

# Change API_PORT from 443 to 8080
sed -i 's/API_PORT=443/API_PORT=8080/' AzureGovAdapter.conf
sed -i 's/API_PROTOCOL=https/API_PROTOCOL=http/' AzureGovAdapter.conf

cat AzureGovAdapter.conf

# Repackage adapter.zip
cd /tmp/adapter-check
zip -r /tmp/azuregov-pak/adapter.zip .

# Repackage the .pak
cd /tmp/azuregov-pak
zip -r /tmp/AzureGovCloud_1.0.0_fixed.pak .

ls -la /tmp/AzureGovCloud_1.0.0_fixed.pak
Then upload /tmp/AzureGovCloud_1.0.0_fixed.pak to Aria Ops.